#!/usr/bin/python

##For small fucntion which is used  one time we can used lambda

g=lambda x:x*2

print "Value of g(3) is ",g(3)

##Other way to write is
print "Other way to write is ",(lambda x:x*2)(3)
